### To run this program you need to install the dependency packages.

> ### you can use yarn: 
```
$ yarn
``` 

> ### or you can use npm:
```
$ npm install
```

### Then just run the script that is in the package.json file
